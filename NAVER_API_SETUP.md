# 네이버 주소 검색 API 설정 가이드

## 1. 네이버 개발자 센터에서 API 키 발급

### 1.1 네이버 개발자 센터 접속
- https://developers.naver.com 접속
- 네이버 계정으로 로그인

### 1.2 애플리케이션 등록
1. "Application" → "애플리케이션 등록" 클릭
2. 애플리케이션 정보 입력:
   - **애플리케이션 이름**: Youniqle 주소 검색
   - **사용 API**: 검색 → 지역 검색 API 선택
   - **서비스 환경**: Web 서비스
   - **서비스 URL**: https://www.grigobio.co.kr (또는 개발용 http://localhost:3000)

### 1.3 API 키 확인
- 등록 완료 후 "Client ID"와 "Client Secret" 확인
- 이 값들을 복사해두세요

## 2. 환경변수 설정

### 2.1 .env.local 파일 생성/수정
프로젝트 루트에 `.env.local` 파일을 생성하고 다음 내용 추가:

```env
# 네이버 주소 검색 API
NAVER_CLIENT_ID=발급받은_Client_ID
NAVER_CLIENT_SECRET=발급받은_Client_Secret

# 기존 환경변수들
NEXTAUTH_SECRET=your_nextauth_secret_here
NEXTAUTH_URL=http://localhost:3000
```

### 2.2 Vercel 환경변수 설정 (배포용)
1. Vercel 대시보드 접속
2. 프로젝트 선택 → Settings → Environment Variables
3. 다음 변수들 추가:
   - `NAVER_CLIENT_ID`: 발급받은 Client ID
   - `NAVER_CLIENT_SECRET`: 발급받은 Client Secret

## 3. API 사용량 및 제한

### 3.1 무료 사용량
- **일일 요청 한도**: 25,000건
- **초당 요청 한도**: 10건
- **월 사용량**: 750,000건

### 3.2 유료 사용량
- 월 750,000건 초과 시 유료
- 상세한 가격은 네이버 개발자 센터에서 확인

## 4. 테스트 방법

### 4.1 로컬 테스트
1. 환경변수 설정 후 개발 서버 재시작
2. 체크아웃 페이지에서 주소 검색 테스트
3. 브라우저 개발자 도구 콘솔에서 API 호출 로그 확인

### 4.2 검색 가능한 주소 예시
- "명일동" → 서울 강동구 명일동 주소들
- "강남구" → 서울 강남구 주소들
- "부산" → 부산광역시 주소들
- "대구" → 대구광역시 주소들
- "인천" → 인천광역시 주소들
- "경기도" → 경기도 주소들

## 5. 문제 해결

### 5.1 API 키가 설정되지 않은 경우
- 로컬 데이터베이스로 자동 폴백
- 주요 지역 주소만 검색 가능

### 5.2 API 호출 실패 시
- 자동으로 로컬 데이터베이스로 폴백
- 안정적인 서비스 제공

### 5.3 검색 결과가 없는 경우
- 다른 검색어로 시도
- 수동으로 주소 입력 가능

## 6. 장점

- ✅ **안정적인 API**: 네이버의 안정적인 인프라
- ✅ **높은 사용량**: 일일 25,000건 무료
- ✅ **빠른 응답**: JSON 형식으로 빠른 처리
- ✅ **전국 커버리지**: 전국 모든 지역 주소 검색
- ✅ **상세 정보**: 건물명, 전화번호 등 추가 정보
- ✅ **폴백 지원**: API 실패 시 로컬 데이터베이스 사용
