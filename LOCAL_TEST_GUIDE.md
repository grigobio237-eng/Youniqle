# 🧪 유니클 리텐션 강화 시스템 로컬 테스트 가이드

이 가이드는 새롭게 추가된 **푸시 알림, 배지 시스템, 커뮤니티 리더보드** 기능을 로컬 환경에서 테스트하기 위한 절차를 설명합니다.

---

### 1. 사전 준비 (Environment)
`.env.local` 파일에 다음 항목들이 정확히 설정되어 있는지 확인하세요.
* `NEXT_PUBLIC_VAPID_PUBLIC_KEY`
* `VAPID_PRIVATE_KEY`
* `CRON_SECRET`
* `MONGODB_URI`

---

### 2. 시나리오 A: 배지 획득 테스트
1. `npm run dev`를 실행합니다.
2. `/ai-navigator` 페이지로 이동하여 **60초 리듬체크**를 완료합니다.
3. `/me` (마이페이지)로 이동합니다.
4. **'회복 성취 뱃지'** 섹션에 `회복의 첫걸음` 뱃지가 추가되었는지 확인합니다.
5. (고급) DB에서 `recovery_scores` 날짜를 조정하여 3일 연속 기록을 만들고 `삼일천하 극복` 뱃지가 나오는지 확인합니다.

---

### 3. 시나리오 B: 푸시 알림 및 넛지 테스트
1. 브라우저에서 `/me` 페이지 하단의 **'푸시 알림 설정'** 스위치를 켭니다. (브라우저 권한 허용 필요)
2. 로컬 서버가 실행 중인 상태에서 다음 `curl` 명령어를 터미널에 입력하여 강제로 넛지를 발생시킵니다.
   ```bash
   curl -X GET "http://localhost:3000/api/notifications/cron-nudge" \
     -H "Authorization: Bearer [CRON_SECRET값]"
   ```
3. 브라우저 또는 OS 알림 센터에 "오늘의 회복 리듬을 체크해보세요!" 메시지가 뜨는지 확인합니다.

---

### 4. 시나리오 C: 리더보드 및 피드 테스트
1. `/me` 페이지 중반부의 **'주간 회복 리더보드'**가 정상적으로 렌더링되는지 확인합니다.
2. 우측의 **'실시간 회복 리듬'** 피드에 본인이 방금 남긴 메타포(은유)가 익명으로 올라오는지 확인합니다.

---

### 💡 주의사항
* **PWA**: 푸시 알림은 가급적 크롬(Chrome) 브라우저에서 테스트하는 것을 권장합니다.
* **DB**: 모든 테스트 결과는 연결된 MongoDB에 저장되므로, 테스트용 계정을 별도로 사용하는 것이 좋습니다.
