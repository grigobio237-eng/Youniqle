---
description: Build the project and push changes to Git automatically.
---
// turbo-all

1. Run the build command
```bash
npm run build
```

2. Add all changes, commit, and push
```bash
git add .
git commit -m "chore: deployment update"
git push
```
